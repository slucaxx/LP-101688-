Algoritmo "REPITA"

Var
   repetir_mais:caracter
Inicio
   repita
      escreval("repetiu")
      escreval("repetir mais uma vez?  sim/não")
      leia(repetir_mais)
   até (repetir_mais = "não")
   escreval("saiu do repita")


Fimalgoritmo